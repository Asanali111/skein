# AGENTS.md — ameliomar

> **Generated by Skein** at 2026-05-20 14:55 UTC. Do not edit this file directly;
> run `skein sync` to regenerate. Add custom content in the `<!-- @user -->` block.

## How to use Skein in this project — read BEFORE you do anything else

Skein is the fastest path to context here. Concrete numbers:
- `project_briefing()` returns the project dashboard in ONE call — <50ms, ~300 tokens.
- `recall("<your task>")` returns top-K relevant fragments — <100ms, ~30 tokens per fragment.
- For comparison: reading 3 source files for the same context costs 3000+ tokens, 5+ round trips, and often misses the WHY.
- Embeddings run locally — no API key needed, your code never leaves this machine.

**Default behavior for any task on this project:**
1. Call `project_briefing()` first if you need the project overview (what's it doing, where it's at, what changed lately).
2. Call `recall("<task>")` for context relevant to your specific task.
3. Only fall back to `read_file` / `grep` for code-execution details that semantic recall can't give.
4. After any non-obvious decision, call `remember(...)` or `note_decision(...)` so other tools (and you in a future session) inherit it.

Each `recall` result carries a `quality` bucket: `high` / `medium` / `low` / `none`. If the top result is `quality=none`, Skein has nothing high-signal for that query — fall back to source. Honest no-knowledge is more useful than weak hits.

---

## Skein context bus

This project uses [Skein](http://127.0.0.1:8765) for cross-LLM context sharing.
MCP endpoint: `http://127.0.0.1:8765/mcp`

Quick reference (MCP tool calls — work in Claude Code, Cursor, Codex, Gemini CLI, Antigravity, etc.):
```
project_briefing(scope="project:ameliomar")              # ONE-call dashboard
recall(query="<task>", scope="project:ameliomar")        # task-specific context
remember(content="<what you decided>", type="decision", scope="project:ameliomar")
```

## Project facts (auto-detected)

Skein extracted these facts from your codebase and recent AI conversations. Treat them as authoritative project context.

### From the codebase

- CI: GitHub Actions (1 workflow file(s) in .github/workflows/). [tags: ci, github-actions]
- Uses Python package `fastembed` (declared: `fastembed>=0.3.0`). [tags: python, dep, fastembed]
- This project's Python package name is `skn`. [tags: python]
- Uses Python package `textual` (declared: `textual>=0.80`). [tags: python, dep, textual]
- Project title (per README.md): `Skein`. [tags: meta]
- Linter: ruff (configured in pyproject.toml). [tags: linting, ruff]
- Test runner: pytest. [tags: testing, pytest]
- Uses Python package `tomli-w` (declared: `tomli-w>=1.0`). [tags: python, dep, tomli-w]
- Uses Python package `tomli` (declared: `tomli>=2.0; python_version < '3.11'`). [tags: python, dep, tomli]
- Uses Python package `watchdog` (declared: `watchdog>=3.0`). [tags: python, dep, watchdog]
- Uses Python package `numpy` (declared: `numpy>=1.24`). [tags: python, dep, numpy]
- Uses Python package `rich` (declared: `rich>=13.7`). [tags: python, dep, rich]
- Uses Python package `httpx` (declared: `httpx>=0.27,<1.0`). [tags: python, dep, httpx]
- Uses Python package `click` (declared: `click>=8.1,<9.0`). [tags: python, dep, click]
- Uses Python package `pydantic` (declared: `pydantic>=2.5,<3.0`). [tags: python, dep, pydantic]
- Uses Python package `uvicorn` (declared: `uvicorn[standard]>=0.27,<1.0`). [tags: python, dep, uvicorn]
- Uses Python package `fastapi` (declared: `fastapi>=0.110,<1.0`). [tags: python, dep, fastapi]
- Python version requirement: `>=3.9`. [tags: python, runtime]
- This project's Python package name is `skein`. [tags: python]

### From recent Claude Code sessions

- before I start writing [tags: transcript, passive, session:a1fa2748-44b4-4e9b-8cf4-dbb19cc5413c]
- on the API key: it's stored in `~/ [tags: transcript, session:a2e36918-76f3-4fdd-bd94-cc5d6c33f10c, passive]
- at Inbox/Test [tags: transcript, session:a2e36918-76f3-4fdd-bd94-cc5d6c33f10c, passive]
- introducing Agent Memory — Cloudflare](https://blog [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- each other's work [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- search_code) — untouched [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- search_code work perfectly [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- note_decision, search_code, claim_lease, release_lease, query_leases [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, note, lease, leases, hook, hooks (install/uninstall/list), config (show/set), scope (create/list), init, serve, sync, agents-md, status, doctor [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- with all the commands with descriptions" [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- you specifically asked for [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- in the Skein repo's `AGENTS [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- with all the commands with descriptions [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Record a decision (convenience alias for remember --type [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Store a context fragment [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, status, doctor [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, note, lease, leases, agents-md, status, doctor) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- its a cli tool that does the main work in the background, the web part is only for the interface [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- on those last three before the plan: **opencode** speaks MCP first-class so it's a normal sync target; **openclaw** I don't recognize (possible typo for `openclaude` / `opencli` / something else — flag [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- is aspirational [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- dated 2026-05-04 claims this was built, but **none of that code is on disk** — it's still v2 [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- framing the choices above so future-me/you can record what was picked [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recording the disk-vs-vault gap (this is fact, not decision) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- aspirational? [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- note_decision, supersede) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- X" in Claude Code on laptop, see it appear in Cursor on desktop within 200ms [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- supersede, …) | Team workspaces | [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Z` style fragments to `skein inbox` for your review [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- slash command [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- to type skein remember, which they didn't [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
*… and 1 more from transcript-claude — call `recall` for the full list.*

## Active decisions

- skein-website 2026-05-20 messaging pass shipped (commit eba3c2a, pushed to main). Six files: Hero (Apache 2.0 footer), WhatItDoes (handoff card promoted to full-width with concrete remember→recall before/after, bench-preview card removed from home), Dogfood (reframed iter-17 baseline vs iter-18+ all-clients-pick-Skein — kills the "doesn't work with Gemini" misread), /bench (winner highlight + horizontal bars + delta labels), /security (pivoted off free-tier/cloud-tier framing per ADR-003 to Apache-2.0-free-forever; crypto section tightened with rotate-token example, dropped stale iter-22.1 promise, added open-source section), /changelog (rebuilt with iter 23-28 entries, vertical timeline, colored nodes, type pills, highlights grid).</content>
<parameter name="rationale">User reviewed screenshots of five panels and gave specific direction on each. Hero MIT line was a stale leftover (real license is Apache 2.0 per ADR-003). Dogfood Gemini panel was creating the wrong impression. Bench card on home was low-value duplication of /bench. Security page tier framing contradicted ADR-003 ("Skein is open source, free, Apache 2.0 — forever"). Cryptography section was a thin paragraph with a 6-month-stale "coming in iter 22.1" promise. Changelog was 5 entries deep, last entry iter 22, missed everything that's shipped since (iter 23 fastembed default, iter 24 quality enum, iter 25 value ranking, iter 26 CLI collapse, iter 27 gemini removal, iter 28 perf + Windows). All changes verified: tsc clean, dev server 200 on all four pages, content spot-check passes.</rationale>
<parameter name="alternatives">(a) Skip the bench-card removal from home and just simplify the numbers — rejected; user asked "simplify or get rid of it" and the full /bench page already exists. (b) Keep the "free tier / cloud tier" framing but rename "cloud tier" to "optional cloud features" — rejected; ADR-003 explicitly kills the tier framing, not just the tier names. (c) Delete the cryptography section entirely — rejected; security-conscious devs still need the bearer-token detail, just tighter than what was there. (d) Split into multiple commits (hero+homepage / bench / security / changelog) — rejected; one user-driven UX pass, atomic commit is more honest about what shipped together.</parameter>
<parameter name="tags">["skein-website", "messaging", "apache-2.0", "ADR-003", "marketing"]
- Skein is locked as open source, free, Apache 2.0 — forever. The four-tier paid Business Model (Personal Pro $9 / Team $18 / Enterprise) dated 2026-05-08 is dead for Skein itself. Adjacent products with separate names may exist as separate businesses, but the Skein project — daemon, CLI, MCP server, embeddings, recall, all of it — has no paid tier and never will. License is Apache 2.0 (patent grant + permissive + corporate-friendly + package-manager-friendly). New distribution plan layers on top of the existing 4-phase Distribution Playbook: adds OSS-specific channels (Lobste.rs, awesome-X PRs, package managers, default-install bundling) and explicit sustainability mechanics (realistic scope cap — 12 visible CLI commands max, <600 tests, <5000 LoC; GitHub Sponsors + Open Collective for symbolic income; contributor pipeline as long-term survival path with ≥3 external contributors merged by Month 6; optional adjacent paid products like cloud-sync-with-separate-name, consulting, training; acquihire/foundation handoff as good endings). Founder survival: 8-week intense launch, then drop to ≤10 hrs/week regardless of trajectory; no support DMs ever, funnel to Issues/Discussions; career capital is the primary founder return. Documented in [[20 Projects/Skein/40_Decisions/ADR-003-open-source-free-forever]] and [[20 Projects/Skein/50_Marketing/OSS Distribution Plan]].
Alternatives considered: (a) Closed-source proprietary SaaS (Mem0-style) — rejected; the entire value proposition is being neutral context infrastructure between commercial AI tools, and a closed daemon reading everything from every coding agent is the opposite product. (b) Open core with paid Team/Cloud tier (the previous Business Model) — rejected; commercial pressure on the OSS core is the classic failure mode (GitLab, Sentry, MongoDB) where users learn to expect free-tier regression; for a neutrality product, every "upgrade to get X" eats trust capital; and the work of running paid infra (auth, billing, SSO, SOC2, support) is 3-6 months of distraction pre-PMF. (c) Source-available (BSL / Elastic-license style) — rejected; kills inclusion in package managers, kills corporate adoption via legal review, kills default-install bundling by other tools; protection-from-AWS-cloning argument doesn't apply to a local-first daemon. (d) Dual-licensed (GPL + commercial) — rejected; same package-manager and adoption costs as (c) plus dual-license dance is a maintenance burden for one person. (e) Foundation handoff (Apache, SFC, NumFOCUS) immediately — deferred; appropriate once there's a real user base and contributor pipeline (~1k stars, 5+ committers); revisit Month 12.
Rationale: Three reasons. (1) The product thesis demands it. Skein's claim is "trusted neutral context between every coding LLM." Any commercial agenda on Skein itself contradicts the claim. The moment a user wonders "is this fragment going somewhere?" or "will recall get worse in the free tier next year?", the product loses. Free + OSS + local-first + Apache-2.0 is the only configuration that makes the trust story unconditional. (2) Distribution wins. Free-forever OSS opens channels paid/source-available products can't reach: Homebrew core, Linux distro repos, awesome-X curation, default-install bundling by other tools, academic citation, conference acceptance, default suggestion in tutorials. These channels compound; the paid-tier roadmap can't access them. (3) Reversibility is asymmetric. Going from "free OSS" to "paid tiers later" is a community betrayal that has killed projects (Audacity, FreeRTOS-pro discontent). Going from "free OSS" to "donate to foundation and walk away" is honorable and clean. Lock in the harder-to-reverse direction now. The sustainability question (how does this not die from solo maintainer burnout in 18 months) is real but separable — addressed via scope cap, contributor pipeline, GitHub Sponsors, optional adjacent paid products, and time-boxed launch — not by paywalling Skein.</rationale>
<parameter name="tags">["distribution", "oss", "license", "apache-2.0", "free-forever", "ADR-003", "business-model", "sustainability"]
- Decided to use X` / `TODO: Y` / `remember that Z` style fragments to `skein inbox` for your review [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Decided to use Memcached") rarely get said cleanly in chat [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Iter 27 SHIPPED — removed the Gemini *embedding* provider entirely. Replaced by fastembed (BAAI/bge-small-en-v1.5, 384-dim, local, no API key) as the only semantic provider, with openai as the opt-in cloud option. The literal string "gemini" is kept as a deprecated alias mapped to FastembedProvider in get_provider() + SkeinConfig.__init__ + load_config() (on-disk rewrite), so legacy configs don't crash the daemon on upgrade. Touched files: skein/embeddings.py (rewrote module docstring with naming-confusion guard, deleted GeminiEmbeddingProvider + 213 LoC of retry/timeout/batch helpers, added alias path + `name` class attr on every provider, BM25 dim 768→384), skein/config.py (removed embedding_dimension from DEFAULTS, normalised alias in __init__, on-disk migration in load_config that runs BEFORE env-var overlay so SKEIN_EMBEDDING_PROVIDER=hash in tests can't mask legacy values), skein/mcp.py (wrapped do_recall, search_chunks, all 3 embed_one writes, and the prompts/get sync recall in asyncio.to_thread — keeps /health responsive during fastembed's 5-15s first-call warm-up), skein/cli.py (--embedding-provider choices = fastembed/openai/bm25/hash, init auto-detect → fastembed, doctor message updates), pyproject.toml (dropped `gemini` extra, dropped google-genai dep), README.md (rewrote provider section), tests/test_embeddings_naming.py (new, 7 guards: alias maps to FastembedProvider, SkeinConfig __init__ normalises, load_config rewrites on disk + strips embedding_dimension, no class starting with Gemini contains "Embedding", get_provider rejects unknown, FastembedProvider class attrs, GeminiCLIClient.id == "gemini_cli"), tests/test_embedding_defaults.py (rewritten — bm25 dim 384, best_available unconditionally fastembed, ignores env keys), tests/test_embeddings_hardening.py (deleted — 240 LoC tested the removed provider). Tests: 551/551 passing on canonical (was 561 - 17 obsolete gemini tests + 16 new = 551 net). Daemon verified end-to-end: cold recall 667ms, warm 199ms, /health 200@9-22ms during parallel remember+embed (event loop stays responsive — the iter-27 root-cause bug "gemini retries wedge asyncio" is gone). Stored 768-dim gemini-era fragments now return zero vectors via bytes_to_vec dim-mismatch fallback so recall still works on them via BM25; full re-ingestion required to lift them into vector-rank.

Alternatives considered: (a) Keep GeminiEmbeddingProvider behind an env-flag opt-out — rejected; the iter-27 fault tree shows the daemon-wedge is the issue regardless of "opt-in", and keeping the class invites future debugging confusion. (b) Auto-detect cloud keys in best_available_provider_name like canonical iter-23 did (GEMINI_API_KEY → gemini > OPENAI_API_KEY → openai > fastembed) — rejected; that's exactly how this user got stuck on gemini in the first place. Cloud opt-in is now explicit via `skein config set embedding_provider openai`, never env-auto. (c) Rename the embedding "gemini" string to "google" to free up "gemini" for the Gemini CLI namespace — rejected as overkill; the GeminiCLIClient already uses `gemini_cli` (underscore) so there's no live collision, and the naming-guard test pins both invariants. (d) Hard-fail get_provider("gemini") instead of aliasing — rejected; would crash launchd's daemon on respawn for any user upgrading from iter ≤26.

Rationale: The user's reported symptom was "skein up doesn't work and is slow" — root cause was their config still naming `embedding_provider=gemini` with a depleted GEMINI_API_KEY. The gemini SDK retry-on-429 ran synchronously inside `async def _call_tool`, blocking the asyncio event loop for ~9–15 s per MCP request → /health timed out at the CLI's 1.5 s probe → ensure_running called launchctl bootstrap, which raced KeepAlive=true respawn → EADDRINUSE cascade for 90+ s. Three compounding bugs (config drift, sync-on-async, launchd race). The chosen fix kills the first two head-on: (1) deletes the provider so the depleted key can't bite, (2) asyncio.to_thread wraps every embed call so even the new fastembed's 5-15 s first-call model-load can't wedge /health (verified empirically — 200 OK in 9-22 ms while a parallel embed was in flight). The launchd race is a separate punch-list item, lower urgency because the daemon now stabilises within ~25 s instead of cascading. The naming-guard test file is the antibody: a future agent who tries to "add Gemini back because the SDK is nice" will hit a CI failure with a comment pointing to this decision. [tags: iter-27, embedding, fastembed, gemini-removal, async-blocking, config-migration, shipped]
Alternatives considered: See content
Rationale: See content [tags: iter-27, embedding, fastembed, gemini-removal, async-blocking, config-migration, shipped]
- ADR-002 phases A-D.1 SHIPPED (iter 26): CLI visible surface collapsed from 50 → 10 commands. Visible: up / down / restart / status / doctor / tail / briefing / tui / config / connect. 27 commands marked hidden=True (still wired, hidden from --help; deletion queued for iter 27 after a week of dogfooding). Daemon-side replacements: auto-sync regenerates AGENTS.md when fragments change (new agents_md_state table + sync_agents_md_for_project helper), inbox auto-approve sweep promotes >=0.85 confidence + auto-rejects after 14 days. New CLI flags: `skein doctor --clean / --reingest`, `skein briefing --since`, `skein connect --remove`. New MCP tools (agent-facing, no CLI): boost(id, value?), bury(id), archaeology(query). Tests: 530 → 561 passing (+21 value, +6 smoke, +4 boost/bury/archaeology); zero regressions. Perf-budget fix: _is_specific_token fused into one compiled regex (1.18ms → 341µs per compute_fragment_value call). README + Commands Reference rewritten.
Alternatives considered: (a) Delete all 27 hidden commands in this session — rejected by advisor; 1,400-LOC deletion + 700-LOC new daemon code = too big a single-session diff with no validation window. (b) Rename `skein hook X` → `skein _hook X` per ADR — rejected; every installed Claude Code .claude/settings.json points at `skein hook` and renaming silently breaks them. Keep current names, just hidden. (c) Doctor's `--reingest` taking an optional path — rejected; Click's nargs="?" is awkward, flag-only form is cleaner; the hidden `skein ingest <path>` is still available for power users.
Rationale: User said "do it all: plan, assess, implement, test, document." Advisor's hide-first-delete-later staging hit the right balance: ships the headline UX win (10-command surface, daemon replacements working) without the risk of a single-session 2,000-LOC diff. Empirical deletion in iter 27 — anything not exercised in 7 days gets removed — is more disciplined than aspirational deletion now. Phase A's daemon replacements are the only piece with real implementation risk; they got the most attention and ship with green tests. [tags: iter-26, ADR-002, cli-collapse, shipped, daemon-side, mcp-tools, vision-aligned]
- ADR-002 (proposed iter 26): collapse the Skein CLI from 50 leaf commands to 10 visible + 4 hidden machine entry-points. Visible surface: up / down / restart / status / doctor / tail / briefing / tui / config / (hidden: remember + recall for debug). Hidden machine entry-points: _serve, _watch, _hook, _init. Deleted (functionality moved): sync, agents-md, gc, note, lease, leases, search, scope create/list, docs sync, hooks install/uninstall/list, chunks list/stats/delete-root/delete-scope, ingest, since, archaeology, inbox + 3 subcommands + auto-approve, events, preview, projects list/remove, daemon status/logs, clients (top-level), disconnect, init. Functionality migrations: (a) daemon background pass for AGENTS.md regen, inbox auto-approve, TTL gc, docs sync. (b) MCP tools only for archaeology, boost, bury, pin/unpin. (c) Existing surface expansion — status gets clients + daemon-state, doctor gets chunks + value-distribution + inbox count + --clean and --reingest flags, tail gets --no-follow + --limit, briefing gets --since, connect gets --remove. Full ADR at 40_Decisions/ADR-002-cli-surface-collapse.md. Net: ~1,400 LOC removed from cli.py, surface 5× smaller, vision-aligned. Estimated 2-3 dev days. Green-field constraint (zero install base) is at its lifetime minimum — every shipped feature ratchets up consolidation cost.
Alternatives considered: (A) Status quo — keep 50 commands. Rejected: violates iter-25 vision constraint directly; each new feature inevitably pushes to 51+. (B) This ADR — aggressive collapse to 10. (C) Mid-ground (~30 commands) — half the work, also half the vision-alignment; second pass to get from 30 → 10 has to happen eventually and is harder once consolidation is half-done.
Rationale: The vision was locked but the existing 50-command surface was inherited from 25 iterations of "ship the command that scratches today's itch." Doing an inventory + ADR is the way to make the vision actually change the codebase rather than just affect future commits. The green-field cost argument is the load-bearing one: with zero install base, breaking deletions are free RIGHT NOW and become progressively expensive. The non-obvious win is that each deletion creates pressure to make the daemon honest — removing `skein sync` forces AGENTS.md regen to be reactive, removing `skein gc` forces the TTL sweep to actually keep the store clean. The constraint is the forcing function. [tags: ADR-002, cli, dx, iter-26, collapse, vision-aligned, D-07]
- Skein vision constraint (locked by user, 2026-05-17): Skein takes a slice of context-sharing work off the user; it MUST NOT grow the surface area the user has to learn. Prefer fewer commands doing more work automatically. New CLI commands are the thing to minimise. New MCP tools (called by agents, not users) are fine because they're part of the LLM's job. When tempted to add a user-facing command, first try: (a) make the daemon do it automatically with sensible defaults, (b) fold the output into an existing command (`skein doctor`, `skein up`, `skein tail`), (c) expose only as an MCP tool the agent can invoke from natural-language. This invalidates the original Q-05 phase 4 proposal (4 new CLI commands: boost/bury/gc/top); rewriting to auto-gc + MCP-only boost/bury + value-distribution-in-doctor.
Alternatives considered: (a) Ship phase 4 as planned with 4 CLI commands — rejected explicitly by user: "we shouldn't expand the toolkit for the user." (b) Make all of phase 4 MCP-only with no human-facing surface — partially adopted; some surfacing in existing commands is still needed for visibility. (c) Skip phase 4 entirely and rely on phase 3 telemetry alone — rejected; user-overrideable signal is sometimes necessary, just not via dedicated commands.
Rationale: Every new command is a thing the user has to learn, remember, and decide when to run. The whole product pitch ("AI tools finally remember each other's work") fails if the user has to babysit the memory layer with 4+ admin commands. The win is invisible context flow — Skein should feel like a daemon that just works, not a CLI suite that demands attention. Concrete consequence for Q-05: phase 4 collapses from 4 new commands into (1) daemon-side auto-gc on the existing stale-mark interval, (2) value distribution + recent gc count surfaced in `skein doctor`, (3) `boost`/`bury` as MCP tools the agent calls when the user says "this is important" / "this is wrong" — no CLI surface at all. Phase 5 (hierarchical tiers) keeps the `pin`/`unpin` MCP tools (agent-facing) but drops any planned user-facing tier-management commands. [tags: vision, constraint, ux, ai-consumer, no-new-commands, user-feedback]
- Q-05 phases 1+2 SHIPPED (iter 25 code side): added `value REAL NOT NULL DEFAULT 0.5` column on fragments + `idx_fragments_value` index. New `skein/value.py` exposes `compute_fragment_value(type, content, extraction_method, created_by_tool, metadata)` returning [0.05, 1.0] from provenance + type + content rubrics. `Storage.create_fragment` calls it on every write; `_migrate_provenance_columns` adds the column to legacy DBs and `_backfill_fragment_values` recomputes values for every existing row in a single transaction. `Fragment` Pydantic model gains a `value: float = 0.5`. `retrieval.recall` hydrates all fused candidates, multiplies RRF score by `frag.value`, re-sorts, then slices `req.limit`. The `quality` enum remains cosine-driven; only `score` carries the value boost.
Alternatives considered: (a) Pre-fusion rank-multiplier (`rank/value` before RRF) — rejected; post-fusion multiplication is one line, easier to test, and equivalent in dominance ordering. (b) Hide low-value fragments by hard filter at recall — rejected for phase 1; down-ranking preserves serendipity and the filter can come later when telemetry calibrates the floor. (c) Per-scope value tables — deferred; current single-column-per-fragment design is enough for the single-user single-machine setup, and phase 3 (telemetry) is where per-scope splits become useful. (d) Compute value from an LLM judge at write-time (Generative Agents / Mem0 style) — rejected; breaks iter-23 local-first promise.
Rationale: Diagnosis was clear: store dominated by tool-event observations and dep-facts; recall returns noise even when retrieval algorithm is correct. The fix has to make valuable content beat noisy content during ranking without deleting either. Provenance is the strongest cheap signal (user-typed remember ≫ passive observation); the type prior and content-score rubrics catch the rest. Post-fusion multiplication keeps RRF semantics intact for in-tier comparisons and only changes cross-tier ordering. Backfill is essential — without it the column would be 0.5 everywhere for legacy rows and the boost would do nothing for existing data. Test suite went from 530 → 551 with 21 new tests covering all three signal families, the migration path, and the end-to-end demote-noisy-fragments-below-valuable-ones invariant. [tags: iter-25, Q-05, value-ranking, provenance, content-score, phase-1, phase-2, shipped]
- Hero animation moved from SVG to canvas: PixelLoom renders a 160×120 logical-pixel scene scaled with image-rendering:pixelated in a single rAF loop. Six pixel-thread heads travel from the right edge toward an 11×11 pixel-art skein sprite. No React state per frame, no SVG filters, no backdrop-blur, no window event listeners.
Alternatives considered: Keep SVG and throttle the mousemove handler with requestAnimationFrame (rejected — fixes the React thrash but leaves the SVG filter cost). Move to WebGL via three.js or pixi (rejected — overkill, adds ~150KB, the pixel-art aesthetic doesn't need 3D). Pre-render the animation as a video/Lottie (rejected — loses interactivity hooks, doesn't honor prefers-reduced-motion cleanly, larger asset). Drop the animation entirely and use a static SVG (rejected — the user explicitly asked for cool design + pixelated motion).
Rationale: Iter 24's SVG ThreadDiagram had four named perf bugs the user could feel: (1) window mousemove handler updated React state on every event, forcing six bezier paths to recompute, (2) feGaussianBlur filter on six animated paths re-rasterized per frame, (3) 12 SMIL animations ran on the main thread, (4) backdrop-blur-sm on six label chips stacked over the SVG paint chain. A canvas at low logical resolution + a single rAF loop eliminates all four classes of work simultaneously. The pixelated aesthetic doubles as on-brand: 'skein' is a coil of yarn, the visual metaphor reads at a glance. A jules.google research agent confirmed they use the same architecture (plain 2D canvas + rAF + IntersectionObserver, no WebGL/Lottie/video). [tags: marketing, website, perf, iter25]
- Q-05 opened (iter 24): fragment-value ranking research. Recommended path is a 5-phase rollout: (1) provenance + type priors at write-time (value REAL DEFAULT 0.5 column, multiply RRF rank by 1/value, ~half day, **start here**); (2) content-score regex pass at write-time (~half day); (3) behavioural telemetry with daily background recompute and 5%/week decay (~2 days); (4) user controls — boost/bury/gc --value-floor/top --by-value (~3 days); (5) hierarchical tiers core/hot/cold (~1 week, defer until phases 1-4 validate). Full deep dive in Obsidian at 20 Projects/Skein/30_Problems/open/Q-05-fragment-value-ranking.md.
Alternatives considered: (a) Read-time recomputation of value — rejected, kills <100ms recall latency. (b) Per-recall LLM-as-judge (Mem0-style) — rejected, breaks iter-23's local-first/zero-API-key story. (c) Jump straight to hierarchical tiers (Letta core/hot/cold) — rejected, big schema change; phases 1-4 reveal what tier boundaries should be. (d) Hard-filter low-value at recall instead of down-rank — deferred, hiding can drown signal at scale; revisit empirically.
Rationale: User observation that motivated this: users start new LLM sessions when context is bad, but they cannot restart Skein. So noisy sessions permanently degrade every future session's recall — exactly the audit finding (store dominated by tool-event observations like "Edit on /path", high-confidence facts stuck in inbox). Iter 24's quality enum surfaces honest signals but doesn't fix the corpus density problem. Fix is to give Skein the wheat-from-chaff job that users currently do by reopening their chat. Multi-axis value (provenance prior + type prior + content score + behavioural signal) is cheap to compute, doesn't need an LLM call per write, and maps directly onto existing primitives (supersede, TTL, extraction_candidates, is_stale). Phase 1 alone is a strict improvement on day one since it can backfill via source_tool. [tags: iter-24, Q-05, value-ranking, noise, research, open]
- Default install path for the skein-website install widget is pipx, marked recommended with a green-dot pill. Tab order: pipx → uv → pip → brew. The pip command was rewritten from `pip install skn && skein up` to `pip install --user skn && hash -r && skein up` because the original fails on a fresh shell.
Alternatives considered: Keep pip default (rejected — it's broken on fresh shells and unsafe without a venv). uv default (rejected — fast and modern, but pipx is more widely installed today and the user base on the marketing site is mostly Python-tool veterans who already have pipx). Make brew default (rejected — Homebrew tap is still pending). Hide all alternatives and only show pipx (rejected — uv users would copy-paste the pipx line into uv and get confused).
Rationale: `pip install skn && skein up` had two latent bugs: (1) pip doesn't refresh shell PATH, so `&& skein up` couldn't find the freshly-installed binary on the same line, and (2) plain pip without a venv pollutes system Python on macOS/Linux. pipx fixes both — isolated env, auto-PATH, single command works on a clean machine. `hash -r` on the pip variant tells bash to drop its command cache so the new binary is discoverable mid-line. Default-install path is what most visitors will follow; it needs to actually work. [tags: marketing, install, website, dx]
- Iter 24: replace opaque RRF score with a `quality` enum (high/medium/low/none) derived from raw cosine similarity. Plumb raw cosine + bm25 alongside the fused score on RecallResult and ChunkSearchResult. Add `skein inbox auto-approve --min-confidence 0.85 [--min-age-days N] [--dry-run]` to drain the extraction-candidate backlog that was poisoning recall scores.
Alternatives considered: (a) Normalise the RRF score itself to [0,1] against its theoretical max. Rejected — RRF is ordinal-rank-only, so even a normalised value doesn't capture semantic similarity strength, just rank position. (b) Keep the existing `score < 0.1` threshold in AGENTS.md. Rejected — with k=60 and 2 ranked lists, RRF maxes at ~0.033, so the threshold was a no-op that the project shipped for ~9 iterations. (c) Daemon-side background auto-approve task. Deferred — start with an opt-in CLI to validate thresholds before automating.
Rationale: Diagnosis (from external Sonnet 4.6 review): recall scores all clustered at 0.016 not because retrieval was broken but because (1) the store was thin and noisy and (2) the surface signal was uninterpretable. Cosine on the iter-23 default fastembed (bge-small-en-v1.5) is the meaningful signal — bucketed thresholds (>=0.65 high, >=0.50 medium, >=0.35 low) make the "honest no-knowledge" promise actually trigger for the first time. The auto-approve drains 173+ stale inbox items so high-confidence facts join the index instead of sitting in review forever. Both changes are surgical and additive; existing `.score` consumers (hooks.py MIN_INJECT_SCORE) still work unchanged. Defers #1 (transcript watcher default-on) and #4-#7 (cold-start git import, `skein since`, push-model hooks, synthesized recall) to follow-on iterations. [tags: iter-24, recall, quality-signal, rrf, inbox, auto-approve]
- Iter 23: switching default embedding provider from "bm25" (HANDOFF.md said gemini but real default in config.py was bm25) to "fastembed" (BAAI/bge-small-en-v1.5, 384-dim, local). Adds name and requires_api_key attributes to EmbeddingProvider base class. Adds peek_embedding_dimension to storage for daemon-startup dimension-mismatch warning.
Alternatives considered: Keep bm25 as default (rejected — README claims semantic search). Make fastembed an opt-in extra (rejected — defeats zero-config promise). Use sentence-transformers (rejected — heavier dependency, slower import).
Rationale: Marketing site queued for "pip install skein && skein up" zero-config launch — Gemini-required default would fail silently to bm25 without API key. fastembed runs locally with no API key, ~130MB one-time download. Hybrid retrieval (BM25 + 384-dim vectors) covers lexical + paraphrase with local quality ~4 MTEB points behind Gemini. [tags: embeddings, iter-23, fastembed]

## Preferences

- Tests live in `tests/` (98 files). [tags: testing, layout]

## Current state

- **docs**: @HANDOFF.md
@AGENTS.md
- **readme**: # 2. Activate in any project

cd ~/Documents/your-project
skein up
```

The PyPI package is named **`skn`** because the natural guess `skein` is already taken by an unrelated Apache project. The CLI binary stays `skein` — install with `skn`, run with `skein`.

Other install paths that work the same:…
- **readme**: # Embedding quality

The default `fastembed` provider runs `BAAI/bge-small-en-v1.5` locally (384-dim, ONNX). First daemon startup downloads ~130 MB of model weights to `~/.cache/fastembed/`; after that every embedding is a sub-15 ms CPU call with no API key and no rate limits. Recall is hybrid: BM25…
- **readme**: # Configuration

Config file: `~/.config/skein/config.json` (created by `skein init`).

| Key | Default | Env override |
|---|---|---|
| `port` | 8765 | `SKEIN_PORT` |
| `host` | 127.0.0.1 | `SKEIN_HOST` |
| `db_path` | `~/.config/skein/skein.db` | `SKEIN_DB_PATH` |
| `embedding_provider` | `fastemb…
- **readme**: # MCP tools (for Claude Code, Cursor, Codex, etc.)

Once `skein up` runs and your client is connected, every MCP-capable LLM gets these tools:

| Tool | Description |
|---|---|
| `project_briefing(scope?)` | One-call project dashboard (300 tokens, <50ms) |
| `recall(query, scope, types?, limit?)` | …
- **readme**: # CLI reference

The visible surface is intentionally small — Skein is an invisible helper, not a CLI suite to learn. Ten commands cover everything a human needs:

```
skein up [path]    Start the daemon, register cwd, wire up detected clients
skein down         Stop daemon + watcher, uninstall hook…
- **readme**: # Interactive control panel

skein tui
```

`skein doctor` has two action flags that absorb what used to be standalone commands:

```bash
skein doctor --clean       # interactive cleanup (replaces `skein gc`)
skein doctor --reingest    # re-index the cwd (replaces `skein ingest`)
```

The daemon tak…
- **readme**: # Live event stream

skein tail
- **readme**: # What's the project state?

skein briefing
skein briefing --since 2d        # what changed in the last 2 days
- **readme**: # `projects list`, `daemon status`, value-distribution, inbox depth)

skein doctor
*… 49 more state fragments — call `recall` for full context.*

<!-- @user -->
<!-- Add any manual context here. This block is preserved across `skein sync`. -->
<!-- /@user -->
